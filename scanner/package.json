{
  "name": "probe-exe-scanner",
  "version": "0.1.0",
  "private": true,
  "description": "Probe.exe local read-only project scanner",
  "scripts": {
    "scan": "tsx src/cli.ts scan",
    "build": "tsc",
    "start": "node dist/cli.js scan"
  },
  "devDependencies": {
    "@types/node": "^20.14.10",
    "tsx": "^4.16.2",
    "typescript": "^5.5.3"
  }
}
